name: matrix_waiting_screen
description: "A waiting screen with matrix effect."
version: 0.0.1
homepage: https://github.com/Alerwann/matrix_loading_screen.git
repository: https://github.com/Alerwann/matrix_loading_screen.git


environment:
  sdk: ">=3.0.0 <4.0.0" 
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:

 